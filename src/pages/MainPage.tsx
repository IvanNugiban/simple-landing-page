import React from 'react';
import Header from "../components/Header";

const MainPage = () => {
    return (
        <div>
            <Header/>
            <main>

            </main>
        </div>
    );
};

export default MainPage;